// create age variable
let age = 30
// calculate is child - if they are 7 or under
//calculate is senior - if they are 65 or older
if (age <= 7) {
    console.log('The child price is $3')
}

if (age >= 65) {
    console.log('The senior price is $4')
}