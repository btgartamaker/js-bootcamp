// === - equality operator
// !== - not equal
// < less than
// > greater than
// <= less than or equal
// >= greater than or equal

let temp = 80

if (temp <= 32) {
    console.log('It is freezing outside!')
}

if (temp >= 110) {
    console.log('It is way to hot outside!')
}

